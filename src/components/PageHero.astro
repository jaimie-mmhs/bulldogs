
---
const { title, subtitle } = Astro.props;
---
<section class="bg-slate-50 border-b">
  <div class="mx-auto max-w-7xl px-4 py-10">
    <h1 class="text-3xl font-semibold">{title}</h1>
    {subtitle && <p class="mt-2 text-slate-600">{subtitle}</p>}
  </div>
</section>