import { site } from '~/site.config';
const nav = site.nav;
---
<header class="bg-bulldog-blue text-white">
  <div class="mx-auto max-w-7xl px-4 py-4 flex items-center justify-between">
    <a href="/" class="font-semibold text-lg tracking-wide">{site.name}</a>
    <nav class="hidden md:flex gap-6">
      {nav.map((item) => (
        <a class="hover:text-bulldog-gold" href={item.href}>{item.label}</a>
      ))}
    </nav>
  </div>
</header>